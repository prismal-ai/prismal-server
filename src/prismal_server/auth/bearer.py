"""``BearerTokenBackend``.

Filled in Phase 4 (RHB-04-03): resolves an ``Authorization: Bearer`` token to an
``AgentIdentity`` (static map or JWT claims → DID/org_id/scopes)
(SPEC-RHB-AUT-003). Static tokens are ``SecretStr``.
"""
